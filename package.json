{
  "name": "m365-mcp-plugin",
  "displayName": "Microsoft 365 MCP (Outlook + Teams)",
  "description": "Da a tu agente de Copilot acceso a tu correo, agenda y chats de Teams via Microsoft Graph, reutilizando la sesion de Microsoft de VS Code. Expone todo como servidor MCP para el chat de IA.",
  "version": "0.2.0",
  "publisher": "lrbg",
  "engines": {
    "vscode": "^1.96.0"
  },
  "categories": [
    "Other",
    "AI"
  ],
  "keywords": [
    "outlook",
    "teams",
    "microsoft 365",
    "graph",
    "mcp",
    "copilot"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/lrbg/MCP_Outlook.git"
  },
  "license": "MIT",
  "main": "./out/extension.js",
  "activationEvents": [
    "onStartupFinished",
    "onView:m365View"
  ],
  "contributes": {
    "mcpServerDefinitionProviders": [
      {
        "id": "m365.mcpServers",
        "label": "Microsoft 365"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "m365",
          "title": "Microsoft 365",
          "icon": "media/m365.svg"
        }
      ]
    },
    "views": {
      "m365": [
        {
          "id": "m365View",
          "name": "Outlook + Teams + Minutas"
        }
      ]
    },
    "configuration": {
      "title": "Microsoft 365 MCP",
      "properties": {
        "m365.graph.clientId": {
          "type": "string",
          "default": "14d82eec-204b-4c2f-b7e8-296a70dab67e",
          "description": "Client ID (App Registration) para el login a Microsoft Graph por navegador/device code. Default: Microsoft Graph PowerShell (preconsentido en muchos tenants, evita el AADSTS65002 de la app de VS Code). Si tu organizacion te da una App Registration propia con Mail/Calendars/Chat, pon aqui su Application (client) ID."
        },
        "m365.graph.tenantId": {
          "type": "string",
          "default": "organizations",
          "description": "Tenant para el login: 'organizations' (cuentas de trabajo), 'common', o el Directory (tenant) ID de tu organizacion para forzarlo."
        },
        "m365.graph.scopes": {
          "type": "array",
          "items": { "type": "string" },
          "default": [
            "User.Read",
            "Mail.ReadWrite",
            "Mail.Send",
            "Calendars.ReadWrite",
            "Chat.ReadWrite",
            "offline_access"
          ],
          "markdownDescription": "Permisos de Graph a solicitar. Si el login muestra 'Necesita aprobacion del administrador' por `Mail.Send` o `Chat.ReadWrite`, quita esos de la lista para conectar con lo que si concede tu tenant (correo/agenda). `offline_access` se agrega solo."
        },
        "m365.mcpScope": {
          "type": "string",
          "enum": [
            "ask",
            "user",
            "workspace"
          ],
          "enumDescriptions": [
            "Preguntar cada vez que haya una carpeta abierta",
            "Registrar el MCP para todo VS Code (recomendado)",
            "Registrar el MCP solo en la carpeta abierta (.vscode/mcp.json)"
          ],
          "default": "ask",
          "description": "Donde se registra el servidor MCP para Copilot Chat. Con 'user' funciona en cualquier ventana, aunque no haya carpeta abierta."
        },
        "m365.polibio.supabaseUrl": {
          "type": "string",
          "default": "",
          "description": "(Opcional) URL de Supabase de PolibioDesk (ej. https://TU-REF.supabase.co) para el conector de minutas (Anotador de Reuniones). Se combina con /functions/v1/minutas-read. Vacio = conector desactivado."
        },
        "m365.polibio.anonKey": {
          "type": "string",
          "default": "",
          "description": "(Opcional) anon key de Supabase de PolibioDesk, requerido como header apikey del gateway de edge functions. El token de lectura se guarda aparte con 'M365: Guardar token de Polibio (minutas)'."
        }
      }
    },
    "commands": [
      {
        "command": "m365.signIn",
        "title": "M365: Iniciar sesion (navegador)"
      },
      {
        "command": "m365.signInDeviceCode",
        "title": "M365: Iniciar sesion (codigo de dispositivo)"
      },
      {
        "command": "m365.refresh",
        "title": "M365: Refrescar sesion"
      },
      {
        "command": "m365.status",
        "title": "M365: Estado y permisos concedidos"
      },
      {
        "command": "m365.registerMcpServer",
        "title": "M365: Registrar servidor MCP (Copilot Chat)"
      },
      {
        "command": "m365.setPolibioToken",
        "title": "M365: Guardar token de Polibio (minutas)"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "vscode:prepublish": "npm run compile",
    "package": "vsce package",
    "test": "node --test mcp/test/"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/vscode": "^1.96.0",
    "typescript": "^5.5.0"
  }
}
